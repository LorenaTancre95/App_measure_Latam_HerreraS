// ARMode is shared between ARCameraViewController and BoxDetectionCoordinator.
// ExpoView-based native view removed — see ARCameraViewController for the new approach.
enum ARMode { case auto, manual }
